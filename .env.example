# Copy to .env — use localhost when running uvicorn on your host (Windows/Mac/Linux).
# In Docker, compose overrides DATABASE_URL and REDIS_HOST for the backend service.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/veducation
REDIS_HOST=localhost
REDIS_PORT=6379
SECRET_KEY=change-me-in-production
